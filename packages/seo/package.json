{
  "name": "@ecom/seo",
  "version": "1.0.0",
  "private": true,
  "main": "dist/generateMetadata.js",
  "types": "dist/generateMetadata.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "lint": "echo 'no lint configured for @ecom/seo'",
    "test": "echo 'no tests configured for @ecom/seo'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
